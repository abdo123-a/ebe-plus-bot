# 🤖 لوحة تحكم بوت تيليجرام (Telegram Bot Control Panel)

نظام إدارة متكامل لبوتات تيليجرام مبني باستخدام **Node.js** و **Express**، مع واجهة مستخدم تفاعلية (Real-time) تعتمد على **Socket.io** وقاعدة بيانات **Firebase Realtime Database**.

يتيح لك هذا المشروع إدارة المحادثات، الرد على المستخدمين، إرسال واستقبال الملفات، وحفظ سجل المحادثات بالكامل سحابياً.

---

## ✨ المميزات الرئيسية

### 🖥️ الواجهة (Frontend)
* **تحديث لحظي (Real-time):** ظهور الرسائل الجديدة فوراً دون تحديث الصفحة.
* **دعم الوسائط الكامل:** عرض الصور، تشغيل الصوتيات، وتحميل المستندات (PDF, ZIP, etc.).
* **تصميم متجاوب (Responsive):** يعمل بكفاءة على الكمبيوتر والموبايل (تصميم يشبه التطبيقات).
* **الوضع الداكن (Dark Mode):** زر للتبديل بين الوضع الليلي والنهاري مع حفظ الإعدادات.
* **بحث سريع:** إمكانية البحث في قائمة المحادثات والرسائل.
* **الإيموجي:** لوحة إيموجي مدمجة وسهلة الاستخدام.
* **تفاعلات متقدمة:**
    * الرد المقتبس (Reply) عبر الضغط المزدوج على الرسالة.
    * مؤشر "جاري الكتابة..." يظهر للمستخدم في تيليجرام.
    * تنبيهات صوتية وشارات للرسائل غير المقروءة.

### ⚙️ النظام والبيانات (Backend)
* **نظام حماية (Login):** صفحة تسجيل دخول بكلمة مرور لحماية لوحة التحكم.
* **تخزين سحابي:** جميع المحادثات وبيانات المستخدمين مخزنة في Firebase.
* **إدارة الرسائل:**
    * **حذف:** إمكانية حذف الرسائل من الطرفين (ومن قاعدة البيانات).
    * **تعديل:** إمكانية تعديل رسائل البوت المرسلة.
* **بروكسي الملفات:** نظام ذكي لتجاوز روابط تيليجرام المؤقتة وتحميل الملفات بأسمائها الأصلية.
* **الاستدامة (Keep-Alive):** مسار `/ping` مخصص لخدمات Uptime لضمان عمل البوت 24/7.

---

## 🛠️ التقنيات المستخدمة

* **Runtime:** Node.js
* **Framework:** Express.js
* **Real-time:** Socket.io
* **Database:** Firebase Admin SDK (Realtime Database)
* **Telegram Integration:** Axios (Direct API calls)
* **File Handling:** Multer & Form-Data

---

## 🚀 طريقة التثبيت والإعداد

### 1. المتطلبات المسبقة
تأكد من وجود التالي لديك:
1.  **Node.js** مثبت (أو استخدام Replit).
2.  **Telegram Bot Token:** احصل عليه من BotFather.
3.  **Firebase Service Account:** ملف JSON يحتوي على مفاتيح الوصول لقاعدة بيانات Firebase.

### 2. إعداد متغيرات البيئة (Environment Variables)
في ملف `.env` أو في إعدادات **Secrets** في Replit، قم بإضافة المتغيرات التالية:

| المفتاح | الوصف | مثال |
| :--- | :--- | :--- |
| `TELEGRAM_TOKEN` | توكن البوت الخاص بك | `123456:ABC-DEF...` |
| `FIREBASE_KEY` | محتوى ملف Service Account (JSON) كاملاً | `{"type": "service_account", ...}` |
| `ADMIN_PASSWORD` | كلمة المرور للدخول للوحة التحكم | `MyStrongPass123` |

> **ملاحظة:** تأكد من أن `FIREBASE_KEY` لا يحتوي على مسافات زائدة وأنه بتنسيق JSON صحيح.

### 3. تشغيل المشروع
لتثبيت المكتبات وتشغيل السيرفر:

```bash
# تثبيت الاعتمادات
npm install

# تشغيل المشروع
node index.js